import 'package:flutter/material.dart';

class HelpTabPage extends StatelessWidget {
  const HelpTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Help Page'),
    );
  }
}
